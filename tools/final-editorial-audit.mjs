import fs from "node:fs";import path from "node:path";
const root=process.cwd(),read=file=>fs.readFileSync(path.join(root,file),"utf8"),app=read("src/App.tsx"),failures=[];
const checks={version:/const APP_VERSION = "10\.6"/.test(app),localPortfolio:/SOLO SU QUESTO DISPOSITIVO/.test(app)&&/senza account e senza sincronizzazione/.test(app),noDeviceSync:!/(BroadcastChannel|WebSocket|sincronizza con (?:il )?(?:PC|telefono))/i.test(app),noServiceNotes:!/(aggiunto nella versione|le (?:nuove )?figure.{0,30}cambiano continuamente|nomi non vengono pronunciati)/i.test(app),learningModules:["realDialogues","listeningProgression","spacedReview","localPortfolio","writingRubric","adaptiveDifficulty"].every(name=>app.includes(name)||fs.existsSync(path.join(root,`src/${name}.ts`)))};
for(const [name,ok] of Object.entries(checks))if(!ok)failures.push(name);console.log(JSON.stringify({engine:"revisione editoriale finale",checks,failures},null,2));if(failures.length)process.exitCode=1;
