import { createServer } from "vite";
import { existsSync, statSync } from "node:fs";
import { resolve } from "node:path";
const server=await createServer({server:{middlewareMode:true},appType:"custom",logLevel:"silent"});
try{const {mobileCurriculum}=await server.ssrLoadModule("/src/curriculum.ts");const levels=Object.fromEntries(["A1","A2","B1","B2","C1"].map(level=>[level,mobileCurriculum.filter(unit=>unit.cefr===level).length]));const missing=[];const invalid=[];for(const unit of mobileCurriculum){if(unit.grammar.explanationIt.length<3||unit.vocabulary.length<5||unit.listening.questions.length<2||unit.quickCheck.length<2)invalid.push(unit.id);const files=[...unit.grammar.examples.map((_,i)=>`${unit.id}-example-${i+1}.wav`),...unit.vocabulary.map((_,i)=>`${unit.id}-vocab-${i+1}.wav`),`${unit.id}-listening.wav`,`${unit.id}-speaking.wav`];for(const file of files){const path=resolve("public/audio",file);if(!existsSync(path)||statSync(path).size<1000)missing.push(file)}}console.log(JSON.stringify({total:mobileCurriculum.length,levels,minMinutes:Math.min(...mobileCurriculum.map(x=>x.minutes)),maxMinutes:Math.max(...mobileCurriculum.map(x=>x.minutes)),invalid,missing},null,2));if(missing.length||invalid.length||mobileCurriculum.length!==60)process.exitCode=1}finally{await server.close()}